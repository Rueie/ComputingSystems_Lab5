version: '3.1'

services:

  shopbd:
    build: postgres/
    restart: always
    ports:
      - 8000:5432
    container_name: shopBD
    volumes:
      - ./postgres/postgresData:/var/lib/postgresql/data/shopInfo
      
  productserv:
    build: golang/product/
    restart: always
    ports:
      - 8010:8010
    container_name: productService
    volumes:
      - ./golang/product/productServiceData:/usr/src/app

volumes:
  postgresData: null
  productServiceData: null